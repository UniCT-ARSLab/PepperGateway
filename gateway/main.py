from WebServer import WebServer
from robot import *
import config
import time

if __name__ == "__main__":
	# pepper = Pepper(config.IP_ADDRESS, config.PORT)
	webServer = WebServer()
	# webServer = WebServer()